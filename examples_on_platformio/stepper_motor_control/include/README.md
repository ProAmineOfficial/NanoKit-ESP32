# Include Folder

**Developed by Amine Saoud ibn al-Bashir.**

This example keeps the stepper sequence in `src/main.cpp` for readability. Add headers here if the motor controller is refactored into reusable classes.

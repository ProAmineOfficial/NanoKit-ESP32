# Stepper Motor Control

PlatformIO example workspace for controlling a stepper motor with NanoKit Integrated ESP32.

Add firmware in `src/`, headers in `include/`, reusable local libraries in `lib/`, tests in `test/`, media in `assets/`, and generated guides in `.genius/`.
